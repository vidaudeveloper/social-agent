# create — 创作层

写稿、配图、视频、管线编排。

命名规范见 [`skills/README.md`](../README.md)。叶子目录名 = `name:`；中层可读。

| 路径 | name / 说明 |
|------|-------------|
| [`pipeline-orchestrator/`](pipeline-orchestrator/) | `pipeline-orchestrator` — 主编排 Step 0–5 |
| [`image/`](image/) | 总览 `img-skills`；叶子 `xhs-card-render`、`img-tokenware` |
| [`video/`](video/) | **视频类型索引**（TTS / Remotion / creative-agent） |
| [`video/tts-narration/`](video/tts-narration/) | 总览 `tts-narration`（弃用）；叶子 `dy-create` / `tt-create` / `yt-create` |
| [`video/remotion/`](video/remotion/) | `remotion` |
| [`video/creative-agent/`](video/creative-agent/) | `creative-agent` |

TTS 口播的 `create-video` CLI 仍在 `skills/publish/{platform}/scripts/`。
