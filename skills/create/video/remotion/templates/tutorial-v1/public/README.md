# public assets

- `voiceover.mp3` — silent placeholder (18s); replace after TTS
- `voiceover-meta.json` — beat timing aligned with `src/data/beats.ts`
- `screenshots/*.png` — placeholder UI (Remotion `<Img>` 对本地 SVG 不稳定，占位用 PNG；真截图也用 PNG)
- `logos/` — placeholder brand mark
- Put emoji SVG / unDraw illustrations here when producing a real tutorial; do not hotlink CDN at render time
