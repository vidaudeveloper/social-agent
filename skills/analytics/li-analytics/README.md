# LinkedIn 数据分析（li-analytics）

技能身份：`li-analytics`（目录与 `name:` 一致）。

```powershell
npm run linkedin:stats
```

需先 `npm run linkedin:setup` 并完成 OAuth。详见 `skills/publish/linkedin/references/linkedin-api-setup.md`。

Agent 技能：[`SKILL.md`](SKILL.md)
