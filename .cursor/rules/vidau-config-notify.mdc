---
description: 变更后主动说明是否需要更新 VidAU 配置；未说明则视为不需要
alwaysApply: true
---

# VidAU 配置更新告知约定

用户约定：**你不说 = 不需要改 VidAU 配置**，用户也不会再追问。

因此，每次完成 skill / profile 相关改动并交付时：

1. **需要**改 VidAU 时，必须主动写明，例如：
   - Hub 技能路径列表要增删/改名
   - `config.yaml` / 模型 / custom_providers
   - profile 安装路径、环境变量、扩展重载以外的客户端设置
2. **不需要**时：交付里可省略「VidAU 配置」段落；不要反问「要不要改配置」。
3. 仅「拉代码 / 更新 skill / 重载 Bridge 扩展」不算改 VidAU 配置；若只有这些，按「不需要」处理，最多一句带过操作步骤即可。
